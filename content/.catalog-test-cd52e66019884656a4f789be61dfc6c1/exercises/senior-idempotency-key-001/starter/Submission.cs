using System;

public static class Submission
{
    public static string ProcessOutcomes(string keys)
    {
        // Parcourez les cles dans l'ordre en retenant celles deja vues.
        // La premiere apparition vaut processed, les suivantes replayed ; refusez une entree nulle.
        throw new NotImplementedException("La distinction traitement / rejeu reste a ecrire.");
    }
}
