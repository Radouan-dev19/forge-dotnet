using System;

public static class Submission
{
    public static string DecompositionAdvice(int teams, int deploysCoupled, int sharedTables)
    {
        // Gardez le monolithe par defaut ; ne justifiez l'extraction que sur des signaux reels.
        throw new NotImplementedException("La decision de decoupage reste a ecrire.");
    }
}
