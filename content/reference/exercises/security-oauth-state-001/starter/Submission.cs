using System;

public static class Submission
{
    public static string StateVerdict(string pendingStates, string consumedStates, string returnedState)
    {
        // Classez le state revenu : absent, déjà servi, en attente, ou jamais émis —
        // et respectez l'ordre de consultation que le contrat impose.
        throw new NotImplementedException("Le verdict sur le state reste à écrire.");
    }
}
