using System;

public static class Submission
{
    // Rend « statut » ou « statut|En-Tête » pour la requête décrite par ses trois attributs.
    public static string ResponseContract(string request)
    {
        throw new NotImplementedException();
    }
}
