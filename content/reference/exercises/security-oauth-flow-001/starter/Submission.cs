using System;

public static class Submission
{
    public static string ChooseFlow(string clientProfile)
    {
        // Analysez le profil — découpage, normalisation, un exemplaire par axe — puis
        // décidez en deux questions : humain présent ? secret gardable ?
        throw new NotImplementedException("Le choix du flux reste à écrire.");
    }
}
