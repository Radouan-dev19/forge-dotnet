using System;

public static class Submission
{
    public static string ResolveApiVersion(string requestPath, string supportedVersions, string defaultVersion)
    {
        // Isolez le premier segment du chemin, reconnaissez s'il porte une version, puis
        // décidez entre servir cette version, la refuser, ou servir le défaut.
        throw new NotImplementedException("La résolution de version reste à écrire.");
    }
}
