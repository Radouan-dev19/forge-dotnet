using System;

public static class Submission
{
    // Classe un findingId en "severite:categorie".
    // Voir le catalogue du brief : blocking:correctness, blocking:security,
    // blocking:concurrency, minor:style, ou unknown ; null leve ArgumentNullException.
    public static string ClassifyFinding(string findingId)
    {
        throw new NotImplementedException("Classez chaque findingId selon le catalogue du brief.");
    }
}
