# Trouver les clients sans commande

Écrivez une requête bornée qui retourne `CustomerId` et `Name` pour les clients n'ayant aucune commande, ordonnés par identifiant.

Le résultat est vide si tous les clients ont commandé. N'utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
