# Joindre lignes et produits

Écrivez une requête bornée qui retourne `Name` et `Quantity` pour les lignes d'une commande donnée, ordonnées par identifiant de produit.

Chaque ligne de la commande produit une ligne de résultat. N'utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
