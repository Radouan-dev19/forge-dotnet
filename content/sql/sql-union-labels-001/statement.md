# Combiner deux vocabulaires

Écrivez une requête bornée qui retourne exactement les colonnes $((Label -join ', ')). L’ordre annoncé est significatif. N’utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
