# Lire un jeton de concurrence pédagogique

Écrivez une requête bornée qui retourne exactement les colonnes $((OrderId DataVersion -join ', ')). L’ordre annoncé est significatif. N’utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
