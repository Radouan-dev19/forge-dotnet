# Conserver les clients sans commande

Écrivez une requête bornée qui retourne `CustomerId` et `OrderCount` pour tous les clients, y compris ceux qui n'ont aucune commande, ordonnés par identifiant.

Un client sans commande doit afficher zéro. N'utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
