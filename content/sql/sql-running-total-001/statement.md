# Calculer un cumul fenêtré

Écrivez une requête bornée qui retourne `OrderId` et `RunningTotal`, cumul des montants dans l'ordre des identifiants.

Chaque ligne porte le cumul des lignes qui la précèdent, elle comprise. N'utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
