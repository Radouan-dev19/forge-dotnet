# Filtrer les commandes ouvertes

Écrivez une requête bornée qui retourne `OrderId` et `CustomerId` pour les commandes ouvertes, ordonnées par identifiant de commande.

Le statut se compare à une valeur exacte. N'utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
