# Joindre paiements et clients

Écrivez une requête bornée qui retourne `OrderId` et `Name` pour les commandes payées et leur client, ordonnées par identifiant de commande.

Chaque commande payée produit exactement une ligne. N'utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
