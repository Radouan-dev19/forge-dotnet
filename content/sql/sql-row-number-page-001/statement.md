# Paginer avec ROW_NUMBER

Écrivez une requête bornée qui retourne `OrderId` et `Position` pour une page de commandes définie par un intervalle de rangs, ordonnée par position.

Le rang se calcule sur l'ordre chronologique. N'utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
