# Classer les totaux par CASE

Écrivez une requête bornée qui retourne `OrderId` et `Band`, tranche du montant de la commande, ordonnée par identifiant.

Les tranches sont contiguës et couvrent tous les montants. N'utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
