# Classer les totaux par CASE

Écrivez une requête bornée qui retourne exactement les colonnes $((OrderId Band -join ', ')). L’ordre annoncé est significatif. N’utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
