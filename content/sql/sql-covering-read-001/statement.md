# Projeter une lecture couverte

Écrivez une requête bornée qui retourne `OrderId` et `Total` pour les commandes payées, ordonnées par identifiant.

Seules ces deux colonnes doivent traverser le réseau. N'utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
