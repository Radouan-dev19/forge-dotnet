# Sélectionner les colonnes de commande

Écrivez une requête bornée qui retourne `OrderId` et `Total` pour toutes les commandes, ordonnées par identifiant.

Aucun filtre n'est demandé, mais l'ordre l'est. N'utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
