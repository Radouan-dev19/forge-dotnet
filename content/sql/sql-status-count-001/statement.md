# Compter par statut

Écrivez une requête bornée qui retourne `Status` et `OrderCount`, nombre de commandes par statut, ordonnée par statut.

Seuls les statuts effectivement présents apparaissent. N'utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
