# Agréger le revenu client

Écrivez une requête bornée qui retourne `CustomerId` et `Revenue`, somme des totaux de commande par client, ordonnée par identifiant.

Seuls les clients ayant au moins une commande apparaissent. N'utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
