# Filtrer avec EXISTS

Écrivez une requête bornée qui retourne exactement les colonnes $((CustomerId Name -join ', ')). L’ordre annoncé est significatif. N’utilisez ni objet serveur, ni référence inter-base, ni donnée externe.
