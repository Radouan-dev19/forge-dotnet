using System;

public static class Submission
{
    // Rend « completed », « compensated » ou « stuck|étape » depuis le journal d'une saga.
    public static string SagaVerdict(string log)
    {
        throw new NotImplementedException();
    }
}
