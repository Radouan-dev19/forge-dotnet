using System;

public static class Submission
{
    // Rend les lignes dans l'ordre canonique done, next, blocker.
    public static string OrderStandup(string update)
    {
        throw new NotImplementedException();
    }
}
