# Squelette C#

```csharp
public static decimal CalculateTotal(IReadOnlyList<decimal> amounts)
{
    // Écrire la proposition dans l'interface de pratique manuelle.
    throw new NotImplementedException();
}
```

Ce fichier sert de point de départ éditorial. Forge.NET ne le compile pas dans l'incrément 04A.
