using System;

public static class Submission
{
    public static string CacheDecision(string entry, int nowSeconds, int staleAfter, int expireAfter)
    {
        // Validez d'abord l'entree et la coherence des seuils, puis calculez l'age
        // et rangez-le dans l'une des trois zones delimitees par staleAfter et expireAfter.
        throw new NotImplementedException("La decision de cache reste a ecrire.");
    }
}
