{
  "schemaVersion": 1,
  "cases": [
    {
      "name": "Visible_FullyHardenedIsCompliant",
      "message": "Les cinq réglages au durci : l'audit rend le mot de conformité, pas une liste vide.",
      "arguments": ["user=app;filesystem=read-only;escalation=blocked;capabilities=dropped;network=bridge"],
      "expected": "compliant",
      "argumentsUnchanged": true
    },
    {
      "name": "Visible_RootAloneIsCritical",
      "message": "Une seule dérive, mais la pire : l'identité racine sort avec la gravité maximale.",
      "arguments": ["user=root;filesystem=read-only;escalation=blocked;capabilities=dropped;network=bridge"],
      "expected": "root-user=critical",
      "argumentsUnchanged": true
    },
    {
      "name": "Visible_TwoGapsKeepSeverityOrder",
      "message": "L'élévation autorisée précède le système de fichiers inscriptible : la gravité ordonne le relevé.",
      "arguments": ["user=app;filesystem=writable;escalation=allowed;capabilities=dropped;network=bridge"],
      "expected": "privilege-escalation=critical;writable-filesystem=medium",
      "argumentsUnchanged": true
    }
  ]
}
