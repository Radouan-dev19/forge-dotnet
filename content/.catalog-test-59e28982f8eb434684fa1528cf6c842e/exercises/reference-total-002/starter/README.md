# Squelette C#

```csharp
public static decimal CalculateDiscountedTotal(
    IReadOnlyList<decimal> amounts,
    decimal discountPercent)
{
    // Écrire la proposition dans l'interface de pratique manuelle.
    throw new NotImplementedException();
}
```
