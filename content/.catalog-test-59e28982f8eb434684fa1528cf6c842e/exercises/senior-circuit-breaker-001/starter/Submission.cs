using System;

public static class Submission
{
    public static string CircuitState(string events)
    {
        // Rejouez la trace sur un automate a trois etats : closed, open, half-open.
        // Le compteur d'echecs ne compte que dans l'etat closed ; rejetez une trace nulle.
        throw new NotImplementedException("L'automate du disjoncteur reste a ecrire.");
    }
}
