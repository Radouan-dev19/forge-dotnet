using System;

public static class Submission
{
    public static decimal LegacyBalance(string ledger)
    {
        // Reconstituez la regle a partir des cas, notamment ce que void annule.
        throw new NotImplementedException("Le calcul du solde hérité reste a ecrire.");
    }
}
