using System;

public static class Submission
{
    // Rend la limite mémoire effective et sa source, au format « valeur|source ».
    public static string EffectiveMemoryLimit(string constraints)
    {
        throw new NotImplementedException();
    }
}
