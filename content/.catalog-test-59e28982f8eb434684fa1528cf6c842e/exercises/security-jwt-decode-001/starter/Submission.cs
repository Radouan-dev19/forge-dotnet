using System;

public static class Submission
{
    public static string ReadClaim(string token, string claim)
    {
        // Découpez le jeton en segments, restaurez l'encodage Base64Url de la charge utile,
        // puis lisez la revendication demandée dans le JSON obtenu.
        throw new NotImplementedException("Décoder la charge utile reste à écrire.");
    }
}
