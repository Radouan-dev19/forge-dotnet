using System;

public static class Submission
{
    // Rend le nombre de segments dont le parent déclaré n'existe pas dans le journal.
    public static int OrphanSpans(string spans)
    {
        throw new NotImplementedException();
    }
}
