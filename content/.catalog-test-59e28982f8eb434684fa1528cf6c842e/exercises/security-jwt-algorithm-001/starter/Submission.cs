using System;

public static class Submission
{
    public static bool UsesRequiredAlgorithm(string token, string requiredAlgorithm)
    {
        // Décodez l'en-tête du jeton, lisez son annonce d'algorithme, puis confrontez-la à
        // l'exigence du vérificateur — sans jamais lui obéir.
        throw new NotImplementedException("La confrontation d'algorithme reste à écrire.");
    }
}
