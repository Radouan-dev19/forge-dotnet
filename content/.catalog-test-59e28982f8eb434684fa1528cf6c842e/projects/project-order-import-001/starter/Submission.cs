using System;

public static class Submission
{
    // Nombre de lignes conformes. L'en-tête et les lignes vides ne comptent pas.
    public static int CountValidRows(string csv)
    {
        throw new NotImplementedException();
    }

    // Une ligne « ligne <n> : <motif> » par ligne rejetée, dans l'ordre du fichier.
    public static string RejectionReport(string csv)
    {
        throw new NotImplementedException();
    }

    // {"valides":n,"rejetees":m,"total":d.dd} — clés dans cet ordre, total arrondi une seule fois.
    public static string ImportReport(string csv)
    {
        throw new NotImplementedException();
    }
}
