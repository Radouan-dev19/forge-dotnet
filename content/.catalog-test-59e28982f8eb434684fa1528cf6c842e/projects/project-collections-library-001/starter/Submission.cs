using System;
using System.Collections.Generic;

public static class Submission
{
    // Assainit puis dédoublonne, en conservant l'ordre de première apparition.
    public static string Normalize(string values)
    {
        throw new NotImplementedException();
    }

    // Compte les occurrences de chaque valeur assainie. La casse distingue deux valeurs.
    public static Dictionary<string, int> Frequencies(string values)
    {
        throw new NotImplementedException();
    }

    // Rend les count valeurs les plus fréquentes ; à égalité, l'ordre ordinal croissant tranche.
    public static string TopValues(string values, int count)
    {
        throw new NotImplementedException();
    }
}
